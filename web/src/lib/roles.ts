/**
 * French labels for the three application roles, shared by every screen
 * that displays a role so they cannot drift apart from one another.
 */
export const ROLE_LABELS = { client: "Client", agent: "Agent", admin: "Administrateur" } as const;
